product: android/app

dependencies:
  - //shared
  - $libs.androidx.activity.compose
  - $compose.preview
  - $compose.uiTooling

settings:
  compose: enabled
  android:
    namespace: nl.petrichor.app
    applicationId: nl.petrichor.app
