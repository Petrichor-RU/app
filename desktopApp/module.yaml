product: jvm/app

dependencies:
  - //shared
  - $compose.desktop.currentOs
  - $compose.preview
  - $libs.kotlinx.coroutines.swing
  - $libs.logback.classic

settings:
  compose: enabled
  jvm:
    mainClass: nl.petrichor.app.MainKt
